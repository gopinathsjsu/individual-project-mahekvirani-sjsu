public class DiscoveryCC extends CreditCard {
    public DiscoveryCC(String cardNumber, String cardHolder, String expirationDate) {
        super(cardNumber, cardHolder, expirationDate);
    }
    // Discovery card specific methods (if any)
}
